export interface ProductProps {
    id: string;
    name: string;
    price: number;
    description: string;
    photo_url: string;
}
export interface ContactProps {
    id: string;
    name: string;
    email: string;
    description: string;
}

